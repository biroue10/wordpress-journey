# Snippets

Small, reusable pieces of code I want to keep — PHP snippets, WP-CLI one-liners, SQL.

Keep each snippet short, commented, and tested. Group by language/tool as the
collection grows (e.g. `php/`, `wp-cli/`, `sql/`).

## Index

_(add snippets as you collect them)_
